<div class="box" style="background-color: #3298db">
    <h3 class="text-light">Brew Post</h3>
    <div class="col-12 col-md-12 mb-5 notification is-link is-light text-light p-3 flex-column">
        <h3 class="portfolio-item-title text-dark">{{post.title}}</h3>
        <h5 class="portfolio-languages text-dark">{{post.body}}</h5>
        <p class="text-dark">posted by {{user.username}} on {{format_date post.created_at}}</p>
        {{#if loggedIn}}
        <hr class="text-dark">
        <form class="comment-form">
            <div>
                <textarea class="textarea" name="comment-body"></textarea>
            </div>
            <br>
            <div>
                <button type="submit">Add Comment</button>
            </div>
        </form>
        {{/if}}
    </div>
    <hr class="text-light">
    <h3 class="text-light">Comments</h3>
    {{#if post.comments}}
    <div class="col-12 col-md-12 mb-5 notification is-link is-light text-dark p-3 flex-column">
        {{> comments post.comments}}
    </div>
    {{/if}}
</div>


{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}