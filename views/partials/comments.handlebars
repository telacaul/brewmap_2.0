<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="text">
            {{comment_text}}
        </div>
        <h4 class="meta text-dark">
            {{user.username}} on {{format_date created_at}}
        </h4>
    </section>
    {{/each}}
</div>